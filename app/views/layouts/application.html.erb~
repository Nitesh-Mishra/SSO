<!DOCTYPE html>
<html>
<head>
  <title>SSO</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
	<div class="container">
<div >
<center>
  <h1>SSO using multiple providers. </h1>
</center>
</div>
		<div class="user_nav well">
			<% if current_user %>
				Signed in as <strong><%= current_user.name %></strong>! from <strong><%= current_user.provider %> </strong> and your id is : <strong><%= current_user.uid %></strong>. &nbsp; 
								
<%=submit_tag 'Sign out', class: 'btn btn-danger' , :onclick=>"javascript:location.href='/sessions/destroy'; return false;" %> &nbsp;
			<% else %>
					<%=submit_tag 'Sign in with Twitter', :name=>'Sign in with Twitter', class: 'btn btn-primary' , :onclick=>"javascript:location.href='/auth/twitter'; return false;" %> &nbsp;&nbsp;&nbsp;
		<%=submit_tag 'Sign in with Github', :name=>'Sign in with Github', class: 'btn btn-primary' , :onclick=>"javascript:location.href='/auth/github'; return false;" %> &nbsp;&nbsp;&nbsp;
		<%=submit_tag 'Sign in with Linkedin', :name=>'Sign in with Linkedin', class: 'btn btn-primary' , :onclick=>"javascript:location.href='/auth/linkedin'; return false;" %> &nbsp;&nbsp;&nbsp;
		<%=submit_tag 'Sign in with Facebook', :name=>'Sign in with Facebook', class: 'btn btn-primary' , :onclick=>"javascript:location.href='/auth/facebook'; return false;" %> &nbsp;&nbsp;&nbsp;
		<%=submit_tag 'Sign in with Google', :name=>'Sign in with Google', class: 'btn btn-primary' , :onclick=>"javascript:location.href='/auth/google_oauth2'; return false;" %>
				
			<% end %>	
		</div>
		<% flash.each do |name, msg| %>
			<%= content_tag :div, msg, id: "flash_#{name}" %>
	    <% end %>
	    <%= yield %>
	</div>
</body>
</html>
