<!DOCTYPE html>
<html>
<head>
  <title>SSO</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
	<div class="container">
		<div class="user_nav">
			<% if current_user %>
				Signed in as <strong><%= current_user.name %></strong>! from <strong><%= current_user.provider %> </strong> and your id is : <strong><%= current_user.uid %></strong>. 
				
				<%= link_to "Sign out", signout_path, id: "sign_out" %>
			<% else %>	
				<%= link_to 'Sign in with Twitter', "/auth/twitter", id: "sign_in"  %> | 
				<%= link_to "Sign in with Github", "/auth/github", id: "sign_in" %> |
				<%= link_to "Sign in with Linkedin", "/auth/linkedin", id: "sign_in" %> |
				<%= link_to "Sign in with Google", "/auth/google_oauth2", id: "sign_in" %> |
				<%= link_to "Sign in with Facebook", "/auth/facebook", id: "sign_in" %> 
			<% end %>	
		</div>
		<% flash.each do |name, msg| %>
			<%= content_tag :div, msg, id: "flash_#{name}" %>
	    <% end %>
	    <%= yield %>
	</div>
</body>
</html>
